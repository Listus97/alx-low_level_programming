#include "main.h"

/**
 * _abs - Computes the absolute value of a number.
 * @i: The number for which the absolute value is calculated.
 *
 * Return: The absolute value of the input number.
 */

int _abs(int i)
{
	if (i > 0)
	return (i);
	else if (i < 0)
	return (-i);
	else
	return (0);
}
